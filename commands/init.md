---
description: Generate or update this project's .agents/pipeline.yaml from the built-in knob registry.
argument-hint: ""
---

Use the `init` skill (`skills/init/SKILL.md`) to handle this invocation. Arguments:

$ARGUMENTS
