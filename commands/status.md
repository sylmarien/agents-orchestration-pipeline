---
description: Show running agent-pipeline pipelines, their current stage, and pending gates.
argument-hint: "[pipeline_id]"
---

Use the `status` skill (`skills/status/SKILL.md`) to handle this invocation. Arguments:

$ARGUMENTS
