---
description: List journaled pipeline decisions and acknowledge or override one.
argument-hint: "[pipeline_id] [--ack <entry_id> | --override <entry_id> \"<redo reason>\"]"
---

Use the `decisions` skill (`skills/decisions/SKILL.md`) to handle this invocation. Arguments:

$ARGUMENTS
