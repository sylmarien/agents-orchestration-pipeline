---
description: Start the agent-pipeline orchestrator on one or more tasks.
argument-hint: <task description> [, in prose: gate preset / check / budget overrides]
---

Use the `run` skill (`skills/run/SKILL.md`) to handle this invocation. Arguments:

$ARGUMENTS
